# LEC. 06 합성곱신경망의 응용

## 합성곱 신경망 모형의 발전

### AlexNet
- 병렬 컴퓨팅을 위해 2개의 GPU로 나누어 계산

**총 가중치 수 계산**

| 층     | 계산식               | 파라미터 수         |
| ----- | ----------------- | -------------- |
| Conv1 | (11×11×3 + 1)×96  | **34,944**     |
| Conv2 | (5×5×96 + 1)×256  | **614,656**    |
| Conv3 | (3×3×256 + 1)×384 | **885,120**    |
| Conv4 | (3×3×384 + 1)×384 | **1,327,488**  |
| Conv5 | (3×3×384 + 1)×256 | **884,992**    |
| FC6   | 256×6×6×4096      | **37,748,736** |
| FC7   | 4096×4096         | **16,777,216** |

### AlexNet의 새로운 시도
- 활성화 함수 개
    - 기존 tanh 대신 ReLU 함수 사용
    - 동일 정확도 기준으로 학습 속도 약 6배 향상
- 데이터 증강: 학습 데이터 다양화로 정확도 개선
- 과대적합 방지: 완전연결층에서 dropout 50% 적용
- 학습 최적화: 학습률 0.9, 배치 사이즈 128, SGD 사용
- 앙상블: 7개의 신경망을 만들어 앙상블로 성능 향상
- GPU 병렬 계산: 2개의 gpu 이용해 병렬 학습
- 지역반응 정규화(LRN): 첫 두 합성곱 연산 후 ReLU 적용 후 정규화 수행

## CNN의 발전 흐름

### 지역반응 정규화
1. 역할
- CNN 안에서 사용되는 정규화 기법 중 하나
- 같은 위치에 있는 뉴런 중 하나가 강하게 활성화되면, 다른 특성맵의 같은 위치 뉴런을 억제
    - **같은 위치끼리 정규화**

2. 효과
- 과도한 활성화 방지, 주변 반응 조절
- ReLU 활성화 규제 역할
- 인간 시각의 '집중' 현상과 유사 

3. 적용 위치
- 1번째, 3번째 합성곱층 이후 
- 현재는 거의 사용되지 않음

### ZFNet
- 기본구조: AlexNet과 동일
- 차이점 
    - Conv1: 7×7 필터, 스트라이드 2로 변경
    - Conv2~4: 필터 수 (512, 1024, 512)
- 개선점
    - 시각화로 CNN 내부 작동을 해석 가능하게 함 -> 딥러닝 블랙박스 해석의 출발점

### VGGNet
- 구조: VGG16, VGG19 (층 수에 따라 이름 구분)
- 특징
    - 단순하고 깊은 구조 -> 재현성 높음
    - 3x3 합성곱 필터만 사용, 스트라이드 1, 패딩 1
    - 층을 깊게 쌓으며 특징 추출력을 강화
    - 단순성 덕분에 여러 모델의 기본 백본으로 활용
- 단점
    - 가중치 수가 지나치게 많아 계산량이 매우 큼
- 전체 가중치 수: 약 1억 3천만 개(AlexNet 두 배)
- 가중치 집중 구간: 마지막 합성곱 -> 완전연결층 연결 부분에 약 75% 존재
- 계산량 절감 방법: 완전연결층 대신 평균 풀링 적용

### GoogLeNet
- 특징
    - 22층 구조
    - 완전연결층 제거 -> 가중치 수 6백만 개
    - Inception 모듈 도입으로 계산 효율 극대화 

#### GoogLeNet의 손실함수

    전체 손실함수 = 최종 손실함수 + 0.3*중간결과1의 손실함수 + 0.3*중간결과2의 손실함수
- 입력데이터로부터 중간결과 2개, 최종결과 1개가 나오도록 하는 구조
- 학습 후 최종 예측값은 최종결과만 사용 

#### GoogLeNet의 인셉션(Inception) 모듈

| 개념             | 설명                                         |
| -------------- | ------------------------------------------ |
| **정의**         | 신경망 안에 여러 크기의 합성곱층을 병렬로 넣어, 다양한 특징을 동시에 추출 |
| **구성요소**       | 1×1, 3×3, 5×5 합성곱 + 3×3 맥스풀링               |
| **핵심 아이디어**    | 각기 다른 크기의 필터로 세밀한/넓은 특징을 동시에 탐색            |
| **1×1 합성곱 역할** | 차원 축소 및 계산량 절감                             |
| **효과**         | 깊이와 폭을 동시에 확보하면서 **가중치 수를 줄이고 성능 향상**      |
| **모듈 수**       | GoogLeNet 전체에 **9개의 인셉션 모듈** 포함            |


#### GoogLeNet의 추가 특징
- 완전연결층 대신 평균 풀링 사용
- 가중치 수: AlexNet의 1/10이지만 정확도는 향상
